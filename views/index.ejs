
    <!-- func.is_user 필요 -->
    <% include partials/navbar %>

        <section class="text-center bg-info text-black justify-content-center align-items-center "
            style="font-size: clamp(32px, 4vw, 70px); padding: 7%;">
            <figure class="slide-in-right">
                <blockquote class="container">
                    <div class="d-flex flex-column flex-md-row align-items-center">
                        <p class="" style="margin-bottom: 0px; font-size : clamp(32px, 4vw, 70px)">
                            완벽한 전문가와 매칭을,&nbsp;
                        </p>
                        <p class="" style="margin-bottom: 0px;font-weight: bolder; font-size : clamp(32px, 4vw, 70px)">
                            링크업에서
                        </p>
                    </div>
                    <p class="mt-2 text-center text-md-start" style="font-size: clamp(15px, 2vw, 24px)">
                        영상 편집자 구인.구직 플랫폼
                    </p>
                </blockquote>
            </figure>
        </section>

        <style>
            @keyframes slideInRight {
                from {
                    opacity: 0;
                    transform: translateX(100px);
                    /* 시작할 때 오른쪽 100px 정도 */
                }
        
                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }
        
            /* 실제로 섹션에 적용할 클래스 */
            .slide-in-right {
                animation: slideInRight 1s ease forwards;
                /* 1초 동안 부드럽게 이동 */
            }
        </style>

        <!-- 카드 섹션 -->

        <% include partials/products %>
        <% include partials/loginmodal %>

            <!-- <script>

                document.querySelector('.postcheck').addEventListener('click', () => {

                    fetch('/post/check')
                        .then(response => response.json())
                        .then(data => {
                            console.log(data);
                            if (data.loggedIn) {
                                window.location.href = '/post';
                                console.log('/post 실행햇다 나도 - 클라이언트');
                            } else {

                                let a = document.querySelector('.posterror');
                                a.innerHTML = '로그인 부터 해라';
                                setTimeout(() => { a.innerHTML = ''; }, 1000);
                            }
                        }).catch(error => console.error("에러 발생", error));
                })


            </script> -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
                integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
                crossorigin="anonymous"></script>


                <!-- <script>
                    window.addEventListener("scroll", function () {
                        let navbar = document.querySelector(".navh");

                        if (window.scrollY > 10) {
                            navbar.style.boxShadow = "0px 4px 10px rgba(0, 0, 0, 0.2)";
                        } else {
                            navbar.style.boxShadow = "none";
                        }
                    });
                </script> -->


</body>

</html>